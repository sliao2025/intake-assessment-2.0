# ===========================================
# INTAKE-ASSESSMENT-V2 Environment Variables
# For Vercel + PlanetScale + Cloudflare R2
# ===========================================

# ============== DATABASE (PlanetScale) ==============
# Get this from PlanetScale dashboard after creating database
# Format: mysql://username:password@host/database?sslaccept=strict
DATABASE_URL="mysql://YOUR_USER:YOUR_PASSWORD@YOUR_HOST/YOUR_DATABASE?sslaccept=strict"

# ============== STORAGE (Cloudflare R2) ==============
# Switch between "s3" (R2/AWS) and "gcs" (Google Cloud Storage)
STORAGE_BACKEND="s3"

# Get these from Cloudflare Dashboard > R2 > Manage R2 API Tokens
S3_ENDPOINT="https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com"
S3_ACCESS_KEY_ID="YOUR_R2_ACCESS_KEY"
S3_SECRET_ACCESS_KEY="YOUR_R2_SECRET_KEY"
S3_BUCKET_NAME="intake-assessment-audio-files"
S3_REGION="auto"

# ============== AUTHENTICATION ==============
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="YOUR_NEXTAUTH_SECRET_HERE"

# Your deployment URL (update after Vercel deployment)
NEXTAUTH_URL="https://your-app.vercel.app"

# Google OAuth (from Google Cloud Console - these still work!)
GOOGLE_CLIENT_ID="YOUR_GOOGLE_CLIENT_ID"
GOOGLE_CLIENT_SECRET="YOUR_GOOGLE_CLIENT_SECRET"

# ============== EXTERNAL SERVICES ==============
# OpenAI API Key (unchanged)
OPENAI_API_KEY="sk-YOUR_OPENAI_API_KEY"

# Email service (Resend)
RESEND_API_KEY="re_YOUR_RESEND_API_KEY"

# ============== INTAKE ANALYSIS SERVICE ==============
# Update this after deploying intake-analysis to Vercel
# This should be the base URL of your Vercel deployment (e.g., https://intake-analysis-xxxxx.vercel.app)
# The API routes will automatically append /api/sentiment and /api/summarize
INTAKE_ANALYSIS_URL="https://your-intake-analysis.vercel.app"
# API Key for secure access to intake-analysis (must match API_SECRET_KEY in intake-analysis)
# Generate with: openssl rand -hex 32
INTAKE_ANALYSIS_API_KEY="your-api-key-here"

# ============== LEGACY GCP (Preserved for future use) ==============
# Uncomment and set STORAGE_BACKEND=gcs to use GCP again
# GCP_PROJECT_ID="your-gcp-project"
# GCS_BUCKET_NAME="intake-assessment-audio-files"
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"
